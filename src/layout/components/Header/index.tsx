import React from 'react';

import HeaderTitle from './components/headerTitle';

const Head = () => {
  return (
    <React.Fragment>
      <HeaderTitle></HeaderTitle>
    </React.Fragment>
  );
};

export default Head;
