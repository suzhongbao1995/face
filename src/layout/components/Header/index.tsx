import React from 'react';

import HeaderTitle from './components/headerTitle';

const Head = () => {
  return (
    <React.Fragment>
      <HeaderTitle />
    </React.Fragment>
  );
};

export default Head;
